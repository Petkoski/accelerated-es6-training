//In ES5:
//for, while, do..while & for..in (to loop through keys/properties in an object)

//New loop to work with (for looping THROUGH ARRAYS):
let testResults = [1.23, 1.10, 4.1];
for (let testResult of testResults) {
    console.log(testResult);
}